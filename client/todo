not remembering?
localStorage


shuffle mode (end)
wadsworth constant (start)
autoplay

use views for entries, playlists -- consistent data, order by across all features


Save settings between refresh--Alpine.persist

probably won't implement right away but PR could be merged:
instant search (ctrl-f works fine until people have more than 5,000 videos or so). With channels like TEDx (over 170,000 videos) this could be useful.
mark all within search as watched
